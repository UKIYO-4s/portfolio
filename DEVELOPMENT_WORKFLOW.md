# 開発ワークフロー - 3 Claude体制

## 概要

このプロジェクトは、3人のClaude AIエージェントが協力して開発を進めます：

- **Claude3（ディレクター）**: プロジェクト全体の統括、タスク管理、レビュー
- **Claude1（ローカル環境）**: `/Users/shoeigoto/Desktop/corporate-site` での開発作業
- **Claude2（サーバー環境）**: `/var/www/portfolio` での本番デプロイ作業

## リポジトリ情報

- **GitHub**: `git@github.com:UKIYO-4s/portfolio.git`
- **メインブランチ**: `main`
- **本番サイト**: https://sd-create.jp

---

## 役割分担

### Claude3（ディレクター）- YOU
**責任範囲:**
- プロジェクト全体の進行管理
- Claude1とClaude2への指示出し
- コードレビューと品質管理
- Git戦略の決定
- タスク優先順位の決定

**作業内容:**
- TodoWrite でタスク管理
- 開発計画の立案
- マージ判断
- リリース承認

---

### Claude1（ローカル環境開発者）
**作業ディレクトリ:** `/Users/shoeigoto/Desktop/corporate-site`

**責任範囲:**
- 新機能の開発
- バグ修正
- テスト実行
- ローカルでの動作確認

**ワークフロー:**
```bash
# 1. 新しい機能ブランチを作成
git checkout -b feature/new-feature

# 2. 開発作業
# - コード編集
# - composer install / npm install
# - php artisan migrate
# - テスト実行

# 3. コミット
git add .
git commit -m "feat: 新機能の説明

🤖 Generated with Claude Code
Co-Authored-By: Claude <noreply@anthropic.com>"

# 4. リモートにプッシュ
git push -u origin feature/new-feature

# 5. Claude3にレビュー依頼
```

**使用コマンド例:**
```bash
# 開発サーバー起動
php artisan serve

# フロントエンド開発
npm run dev

# データベースマイグレーション
php artisan migrate

# テスト実行
php artisan test
```

---

### Claude2（サーバー環境運用者）
**作業ディレクトリ:** `/var/www/portfolio`

**責任範囲:**
- 本番環境へのデプロイ
- サーバー設定管理
- データベース管理
- SSL証明書管理
- ログ監視

**ワークフロー:**
```bash
# SSH接続（必要に応じて）
ssh user@163.44.114.42

# 1. mainブランチの最新を取得
cd /var/www/portfolio
git pull origin main

# 2. 依存関係の更新
composer install --no-dev --optimize-autoloader
npm install
npm run build

# 3. データベースマイグレーション（必要な場合）
php artisan migrate --force

# 4. キャッシュクリア
php artisan config:clear
php artisan cache:clear
php artisan view:clear
php artisan route:clear

# 5. 最適化
php artisan config:cache
php artisan route:cache
php artisan view:cache

# 6. パーミッション確認
chown -R www-data:www-data storage bootstrap/cache
chmod -R 775 storage bootstrap/cache

# 7. サービス再起動（必要な場合）
sudo systemctl restart php8.2-fpm
sudo systemctl restart nginx
sudo systemctl restart corporate-queue
```

**監視コマンド:**
```bash
# ログ確認
tail -f storage/logs/laravel.log

# Nginxログ
sudo tail -f /var/log/nginx/error.log

# キュー状態確認
php artisan queue:work --once

# SSL証明書確認
sudo certbot certificates
```

---

## Git ブランチ戦略

### ブランチ構成

```
main (本番環境)
  ├── develop (開発統合ブランチ)
  │   ├── feature/new-feature-1
  │   ├── feature/new-feature-2
  │   └── bugfix/fix-issue-123
  └── hotfix/critical-bug (緊急修正用)
```

### ブランチ命名規則

- `feature/機能名` - 新機能開発
- `bugfix/修正内容` - バグ修正
- `hotfix/緊急修正内容` - 本番緊急修正
- `refactor/リファクタリング内容` - コード改善
- `docs/ドキュメント内容` - ドキュメント更新

### ブランチ運用ルール

1. **feature/bugfixブランチ**
   - `develop` から分岐
   - 開発完了後、`develop` にマージ
   - Claude1が作業

2. **developブランチ**
   - 開発版の統合ブランチ
   - テスト完了後、`main` にマージ
   - Claude3が承認

3. **mainブランチ**
   - 本番環境反映用
   - 常にデプロイ可能な状態を保つ
   - Claude2がデプロイ

4. **hotfixブランチ**
   - `main` から直接分岐
   - 修正後、`main` と `develop` 両方にマージ
   - Claude3承認後、Claude2が即座にデプロイ

---

## 開発フロー

### 通常の機能開発フロー

```
[Claude3: ディレクター]
  ↓ タスク指示
[Claude1: ローカル開発]
  1. feature/xxx ブランチ作成
  2. コード実装
  3. ローカルテスト
  4. コミット & プッシュ
  ↓ レビュー依頼
[Claude3: コードレビュー]
  5. コード品質チェック
  6. 承認 or 修正依頼
  ↓ 承認後
[Claude1: マージ作業]
  7. develop へマージ
  ↓ リリース判断
[Claude3: リリース承認]
  8. develop → main マージ
  ↓ デプロイ指示
[Claude2: 本番デプロイ]
  9. git pull origin main
  10. ビルド & マイグレーション
  11. キャッシュクリア
  12. 動作確認
  ↓ 完了報告
[Claude3: デプロイ確認]
  13. 本番動作確認
  14. タスク完了
```

### 緊急修正フロー（Hotfix）

```
[Claude3: 緊急バグ検知]
  ↓ 緊急対応指示
[Claude1: Hotfix作成]
  1. hotfix/xxx ブランチ作成（mainから）
  2. 最小限の修正
  3. テスト
  4. コミット & プッシュ
  ↓ 緊急レビュー
[Claude3: 迅速レビュー]
  5. 修正内容確認
  6. 承認
  ↓ 即座にマージ
[Claude1: マージ実行]
  7. main へマージ
  8. develop へもマージ（同期）
  ↓ 緊急デプロイ指示
[Claude2: 緊急デプロイ]
  9. git pull
  10. 最小限のビルド
  11. 即座にデプロイ
  12. 動作確認
```

---

## コミットメッセージ規約

### フォーマット

```
<type>: <subject>

<body>

🤖 Generated with Claude Code
Co-Authored-By: Claude <noreply@anthropic.com>
```

### Type一覧

- `feat`: 新機能
- `fix`: バグ修正
- `docs`: ドキュメント
- `style`: コードスタイル（動作に影響なし）
- `refactor`: リファクタリング
- `perf`: パフォーマンス改善
- `test`: テスト追加・修正
- `chore`: ビルドプロセス、補助ツールの変更

### 例

```
feat: Add user profile editing functionality

- Implemented profile edit form
- Added image upload feature
- Validated input fields

🤖 Generated with Claude Code
Co-Authored-By: Claude <noreply@anthropic.com>
```

---

## 環境変数管理

### ローカル環境（Claude1）
- `.env` - ローカル開発用設定
- データベース: SQLite or PostgreSQL（ローカル）
- Stripe: テストキー
- APP_DEBUG=true

### 本番環境（Claude2）
- `.env` - 本番環境設定（Git管理外）
- データベース: PostgreSQL 15
- Stripe: 本番キー（承認後）
- APP_DEBUG=false

**重要:** `.env` ファイルは絶対にGitにコミットしない

---

## デプロイチェックリスト

### Claude1（デプロイ前準備）
- [ ] すべてのテストが通過
- [ ] マイグレーションファイルの確認
- [ ] .env.example の更新
- [ ] composer.json / package.json の依存関係確認
- [ ] ビルドエラーがないか確認（npm run build）

### Claude3（デプロイ承認）
- [ ] コードレビュー完了
- [ ] 機能動作確認
- [ ] セキュリティチェック
- [ ] パフォーマンス影響確認
- [ ] デプロイタイミングの確認

### Claude2（デプロイ実行）
- [ ] データベースバックアップ
- [ ] git pull 実行
- [ ] composer install --no-dev
- [ ] npm run build
- [ ] php artisan migrate --force
- [ ] キャッシュクリア
- [ ] パーミッション確認
- [ ] サービス再起動
- [ ] 動作確認（主要ページアクセス）
- [ ] エラーログ確認

---

## ロールバック手順

### 緊急時のロールバック（Claude2実行）

```bash
# 1. 前回のコミットに戻す
cd /var/www/portfolio
git log --oneline -5  # コミット履歴確認
git reset --hard <前回の安定コミットID>

# 2. 依存関係を元に戻す
composer install --no-dev
npm install
npm run build

# 3. データベースロールバック（必要な場合）
php artisan migrate:rollback --step=1

# 4. キャッシュクリア
php artisan cache:clear
php artisan config:clear
php artisan view:clear

# 5. サービス再起動
sudo systemctl restart php8.2-fpm nginx

# 6. 動作確認
curl -I https://sd-create.jp
```

---

## コミュニケーションプロトコル

### Claude1 → Claude3 への報告

```markdown
## 完了報告

**タスク:** [タスク名]
**ブランチ:** feature/xxx
**変更内容:**
- ファイル1: 変更内容
- ファイル2: 変更内容

**テスト結果:**
- [x] ユニットテスト通過
- [x] ローカル動作確認

**レビュー依頼ポイント:**
- セキュリティ面での確認
- パフォーマンスへの影響

**次のアクション:** レビュー後、developへのマージをお願いします
```

### Claude3 → Claude2 へのデプロイ指示

```markdown
## デプロイ指示

**対象ブランチ:** main
**コミットID:** abc1234
**変更内容サマリ:**
- 新機能追加
- バグ修正

**注意事項:**
- マイグレーション実行が必要
- キャッシュクリア必須

**確認項目:**
- /shop ページの動作
- 決済フローの確認

**デプロイ後:** 完了報告をお願いします
```

### Claude2 → Claude3 への完了報告

```markdown
## デプロイ完了報告

**デプロイ日時:** 2025-11-23 14:30 JST
**コミットID:** abc1234
**実行内容:**
- [x] git pull 完了
- [x] composer install 完了
- [x] npm run build 完了
- [x] マイグレーション実行
- [x] キャッシュクリア
- [x] サービス再起動

**動作確認:**
- [x] トップページ: 正常
- [x] ショップページ: 正常
- [x] 管理画面: 正常

**エラーログ:** 異常なし

**ステータス:** デプロイ成功
```

---

## トラブルシューティング

### よくある問題と対処法

**問題1: マイグレーションエラー**
```bash
# Claude2実行
php artisan migrate:status  # 状態確認
php artisan migrate:rollback --step=1  # ロールバック
php artisan migrate --force  # 再実行
```

**問題2: パーミッションエラー**
```bash
# Claude2実行
sudo chown -R www-data:www-data storage bootstrap/cache
sudo chmod -R 775 storage bootstrap/cache
```

**問題3: キャッシュ関連エラー**
```bash
# Claude2実行
php artisan cache:clear
php artisan config:clear
php artisan route:clear
php artisan view:clear
```

**問題4: Composerエラー**
```bash
# Claude1/Claude2実行
composer clear-cache
composer install
```

---

## セキュリティガイドライン

### コード実装時（Claude1）
- ユーザー入力は必ずバリデーション
- SQLインジェクション対策（Eloquent使用）
- XSS対策（Blade {{ }} 使用）
- CSRF対策（@csrf 使用）
- ファイルアップロードの検証

### デプロイ時（Claude2）
- .env ファイルのパーミッション確認（600）
- storage ディレクトリのパーミッション（775）
- APP_DEBUG=false 確認
- エラーログの定期確認
- SSL証明書の有効期限確認

### レビュー時（Claude3）
- 認証・認可のチェック
- 機密情報の漏洩チェック
- サードパーティライブラリの脆弱性確認
- APIキーのハードコーディングチェック

---

## パフォーマンス最適化

### Claude1（開発時）
- Eloquentクエリの最適化（N+1問題回避）
- 画像の最適化（Intervention Image使用）
- 不要なコードの削除
- ページネーションの実装

### Claude2（本番環境）
- OPcacheの有効化
- Redis/Memcachedの活用（将来的）
- Nginxのgzip圧縮
- ブラウザキャッシュ設定
- CDN利用（将来的）

---

## バックアップ戦略

### データベースバックアップ（Claude2）
```bash
# 日次バックアップ（Cronで自動化推奨）
pg_dump corporate_site > backup_$(date +%Y%m%d).sql

# バックアップの保存先
/var/backups/portfolio/
```

### ファイルバックアップ
```bash
# アップロードファイル
tar -czf storage_backup_$(date +%Y%m%d).tar.gz storage/app/public/

# 保存先
/var/backups/portfolio/
```

---

## モニタリング

### Claude2が定期確認すべき項目
- エラーログ: `storage/logs/laravel.log`
- Nginxログ: `/var/log/nginx/error.log`
- ディスク使用量: `df -h`
- SSL証明書期限: `sudo certbot certificates`
- キューワーカー稼働: `sudo systemctl status corporate-queue`

---

## 緊急連絡先

- GitHub Issues: https://github.com/UKIYO-4s/portfolio/issues
- 本番サイト: https://sd-create.jp
- サーバーIP: 163.44.114.42

---

**最終更新:** 2025-11-23
**管理者:** Claude3 (Director)
